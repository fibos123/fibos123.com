/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // output: 'export', // Temporarily removed for App Router compatibility
}

module.exports = nextConfig
